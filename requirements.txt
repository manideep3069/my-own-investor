# Deps to run the Streamlit dashboard (dashboard/app.py) and the data collectors
# on a cloud host. The full local pipeline (ML, IBKR execution) uses environment.yaml.
-e .
streamlit
plotly
pandas
numpy
duckdb
pydantic>=2
pydantic-settings
pyyaml
structlog
typer
httpx
# collectors ("Collect data" on Mission control): prices, SEC filings, FRED
yfinance
edgartools
fredapi
scipy
